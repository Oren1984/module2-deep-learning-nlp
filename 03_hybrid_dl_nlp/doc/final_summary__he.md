# 🧠 Hybrid DL + NLP — סיכום פרויקט סופי  
### 📩 זיהוי הודעות ספאם (TF-IDF + MLP)

---

## 🔍 סקירה כללית

פרויקט זה מיישם **צינור עיבוד NLP מקצה לקצה** לזיהוי הודעות ספאם בצורה ריאלית ומעשית.  
הדגש הוא על **בהירות הנדסית, חשיבה ארכיטקטונית ושחזוריות**, ולא על ניסויים אקדמיים.

הצינור משלב בכוונה **טכניקות NLP קלאסיות** יחד עם **מודל למידה עמוקה**, ומשקף אופן עבודה נפוץ במערכות אמיתיות:

> **מתחילים פשוט → מאמתים הנחות → מוסיפים Deep Learning רק כשיש לכך הצדקה**

כל העיבוד הכבד (הכנת נתונים, אימון, הערכה) מבוצע באמצעות **סקריפטים בפייתון**.  
הריפוזיטורי חושף **תוצאות וארטיפקטים**, בעוד שה־Notebook משמש **להסקה ובדיקות בלבד**.

---

## 🎯 הגדרת המשימה

| היבט | תיאור |
|------|-------|
| **בעיה** | סיווג טקסט בינארי (Spam לעומת Ham) |
| **מאגר נתונים** | SMS Spam Collection (ניקוי ופיצול) |
| **סוג למידה** | למידה מונחית |
| **תחום** | NLP יישומי |

---

## 🧩 למה נבחרה הגישה ההיברידית (רציונל תכנוני)

הפרויקט תוכנן **במכוון** כצינור **היברידי של NLP ולמידה עמוקה**,  
ולא כפתרון מבוסס Deep Learning בלבד או Transformers.

---

### 🟦 למה TF-IDF?

הבחירה ב־TF-IDF נעשתה מתוך התאמה **לאופי הנתונים ולבעיה**:

- מאגר הנתונים **קטן, מובנה וממוקד**
- TF-IDF מספק:
  - ביצועים חזקים בבעיות NLP קלאסיות
  - יכולת הסבר ופרשנות מלאה
  - אימון מהיר ועלות תשתית נמוכה
- מאפשר יצירת **קו בסיס ברור וניתן להסבר**, קריטי במערכות פרודקשן

> TF-IDF משמש כאן כמרחב פיצ’רים מבוקר — לא כקיצור דרך.

---

### 🟩 למה Logistic Regression כמודל בסיס?

מודל הבסיס נבחר מסיבות הנדסיות ברורות:

- אימות:
  - איכות הנתונים
  - לוגיקת הניקוי
  - תקינות הפיצ’רים
- יצירת **נקודת ייחוס לביצועים**
- הוכחה שהבעיה ניתנת לפתרון **עוד לפני הכנסת Deep Learning**

> אין הצדקה למודל עמוק בלי קו בסיס יציב.

---

### 🟨 למה MLP (ולא Transformers)?

הבחירה ב־**MLP מעל וקטורי TF-IDF** היא בחירה מודעת ופרגמטית:

- מיקוד ב־**הנדסה יישומית**, לא במורכבות מודלים
- MLP מאפשר:
  - גבולות החלטה לא־לינאריים
  - הדגמה של עקרונות DL:
    - לולאות אימון
    - רגולריזציה
    - Early Stopping
- הימנעות מ:
  - עלות חישובית גבוהה
  - מודלים אטומים
  - Overengineering

> כך נבנות מערכות אמיתיות: מוסיפים DL בהדרגה, לא כברירת מחדל.

---

### 🟥 למה לא Embeddings מקצה לקצה?

צינורות מבוססי Embeddings נזנחו **בכוונה**:

- למרות העוצמה, הם:
  - פחות שקופים
  - קשים יותר לדיבוג
  - בעלי מצבי כשל פחות ברורים
- הפרויקט שם דגש על:
  - שליטה
  - שקיפות
  - שחזוריות

> המטרה היא ביטחון הנדסי — לא רדיפה אחרי ציונים.

---

## ⚙️ סיכום הגישה

- **קדם־עיבוד טקסט:** ניקוי ונרמול  
- **הנדסת פיצ’רים:** TF-IDF (Bag-of-Words)  
- **מודלים:**
  - בסיס → Logistic Regression (TF-IDF)
  - סופי → MLP (PyTorch) מעל TF-IDF  
- **שיטת הערכה:** סט בדיקה נפרד (TEST)

---

## 🔄 שלבי הצינור

1. הכנת מאגר הנתונים  
2. פיצול לאימון / ולידציה / בדיקה  
3. קדם־עיבוד טקסט  
4. וקטוריזציית TF-IDF  
5. מודל בסיס (Logistic Regression)  
6. אימון MLP עם Early Stopping  
7. כיוונון רגולריזציה  
8. הסקה (Inference)  
9. הערכה על סט הבדיקה  
10. הפקת דוח סופי  

📁 כל השלבים מבוצעים באמצעות סקריפטים תחת `src/`.

---

## 📊 תוצאות (סט בדיקה)

- **דיוק:** ~0.98  
- **מדדים:** Precision, Recall, F1-score  

### 📦 ארטיפקטים שנוצרו
- מטריצת בלבול  
- דוח סיווג  
- מדדים מסודרים (joblib)  

---

## 📓 מדיניות שימוש ב־Notebook

ה־Notebook (`notebooks/demo.ipynb`) מיועד **אך ורק** ל:

- דוגמאות הסקה  
- בדיקות sanity  
- עיון בתוצאות  

🚫 **לא משמש לאימון או לכיוונון היפר־פרמטרים.**

---

## 🧠 פילוסופיית תכנון

- מינימלי אך שלם  
- צינור מבוסס סקריפטים  
- תוצאות ניתנות לשחזור  
- הפרדה ברורה בין:
  - אימון
  - הערכה
  - הסקה  
- שימוש ב־Deep Learning **מתוך כוונה**, לא כאוטומט

---

## 🏁 הערת סיום

פרויקט זה מייצג **תהליך NLP היברידי מוכוון פרודקשן**, ומדגים כיצד:

- NLP קלאסי  
- למידה עמוקה  
- משמעת הנדסית  

יכולים להתקיים יחד במערכת **נקייה, נשלטת וניתנת להרחבה**.

הדגש הוא על **קבלת החלטות**, לא רק על מימוש.
