# 📌 סיכום סופי — למידה עמוקה קלאסית (MLP)

## 📍 סקירת הפרויקט
פרויקט זה מדגים **צינור עבודה (Pipeline) של למידה עמוקה בצורה נקייה, מבוקרת ומסודרת**  
לבעיית סיווג רב־מחלקתי של תמונות, באמצעות **רשת עצבית מסוג MLP (Multi-Layer Perceptron)**.

המיקוד בפרויקט הוא **הבנה מלאה של מחזור החיים של אימון מודל**:
טעינת נתונים, הגדרת מודל, אימון עם רגולריזציה, הערכה,
מדידת ביצועי חיזוי (Inference), והפקת דוחות מסודרים —  
ולא השגת דיוק מקסימלי בכל מחיר.

---

## 🎯 מטרת הפרויקט
לאמן ולהעריך מודל MLP קלאסי על בסיס מאגר הנתונים **Fashion-MNIST**,  
תוך שמירה על **הפרדת אחריות בסגנון תעשייתי**:
- אימון המודל באמצעות סקריפטים
- הערכה ודיווח באמצעות סקריפטים
- הדגמה באמצעות Notebook (ל־Inference בלבד)

---

## 📊 מאגר הנתונים
**Fashion-MNIST**
- 70,000 תמונות בגווני אפור (28×28)
- 10 מחלקות של פריטי לבוש
- מאגר מאוזן ומתאים לניסויי DL מבוקרים

המחלקות כוללות:
חולצה, מכנס, סוודר, שמלה, מעיל, סנדל, חולצה ארוכה, נעל ספורט, תיק, מגף

---

## 🧠 ארכיטקטורת המודל
**רשת עצבית מסוג MLP (Multi-Layer Perceptron)**

- קלט: 784 פיצ’רים (תמונה משוטחת)
- שכבה חבויה 1: 256 נוירונים
- שכבה חבויה 2: 128 נוירונים
- פונקציית אקטיבציה: ReLU
- רגולריזציה:
  - Dropout (0.2)
  - Batch Normalization
- פלט: 10 לוגיטים (Softmax מתבצע דרך פונקציית האובדן)

בחירה זו נעשתה בכוונה כדי:
- לשמור על מודל קריא ובר־הבנה
- להמחיש תופעות של Overfitting
- להדגיש טכניקות לייצוב תהליך האימון

---

## ⚙️ הגדרות אימון
- אלגוריתם אופטימיזציה: **Adam**
- קצב למידה (Learning Rate): `0.001`
- פונקציית אובדן: **CrossEntropyLoss**
- גודל Batch: `64`
- מספר Epochs מקסימלי: `20`
- Early Stopping:
  - סבלנות (Patience): `3`
  - מבוסס על Validation Loss
- סביבת הרצה: **CPU**

האימון בוצע באמצעות סקריפטים עצמאיים (ולא דרך Notebook),
בהתאם לפרקטיקות מקובלות בסביבות Production ו־MLOps.

---

## 📈 תוצאות
- **דיוק ולידציה מיטבי:** `~0.8957`
- **דיוק על סט הבדיקה (Test):** `~0.8929`

תוצאות אלו נחשבות טובות מאוד עבור רשת Fully Connected
על Fashion-MNIST, ללא שימוש בשכבות קונבולוציה.

---

## 🧪 תוצרי הערכה (Artifacts)
נוצרו באופן אוטומטי התוצרים הבאים:

- גרפי אימון:
  - `outputs/figures/loss_curves.png`
  - `outputs/figures/acc_curves.png`
- מטריצת בלבול (Confusion Matrix):
  - `outputs/figures/confusion_matrix.png`
- Checkpoint של המודל:
  - `outputs/models/best_model.pt`
- תוצאות מובנות:
  - `outputs/results/result.txt`
  - `outputs/results/train_history.json`
  - `outputs/results/eval_results.json`

---

## ⚡ ביצועי Inference
מדידות בוצעו על CPU:

- חיזוי Batch (256 דגימות): **~1.2 מילישניות**
- זמן חיזוי לדגימה בודדת: **~0.005 מילישניות**

תוצאות אלו מדגישות את היעילות של מודלי MLP
בתרחישים הדורשים Latency נמוך.

---

## 📓 שימוש ב־Notebook
ה־Notebook (`notebooks/demo.ipynb`) מוגדר בכוונה כ־**קריאה בלבד**:
- ללא אימון
- ללא כוונון היפר־פרמטרים
- כולל רק:
  - טעינת מודל מאומן
  - הצגת תחזיות
  - מדידת זמן חיזוי
  - בדיקות sanity בסיסיות

הפרדה זו משקפת תהליכי עבודה מקובלים בעולם האמיתי.

---

## 🧠 תובנות מרכזיות
- הפרדה ברורה בין אימון, הערכה ו־Inference היא קריטית
- Early Stopping משפר הכללה בצורה משמעותית
- רגולריזציה (Dropout + BatchNorm) מייצבת אימון של MLP
- גם ארכיטקטורות פשוטות יכולות להגיע לתוצאות חזקות עם משמעת הנדסית
- סקריפטים עדיפים על Notebooks לצורך אימון שניתן לשחזור

---

## ✅ סטטוס הפרויקט
✔ בוצע במלואו  
✔ ניתן לשחזור מלא  
✔ מבנה תואם סטנדרט תעשייתי  
✔ מוכן להצגה בפורטפוליו או ב־GitHub
